#include <iostream>

using namespace std;

int main()
{
    int m, n;
    cin >> n;
    cin >> m;
    int a[10][10];

    int i, j;
    for (i = 0; i < m; ++i) {
        cout << i  << endl;
        for (j = 0; j < n; ++j) {
            cin >> a[i][j];
        }
    }

    int m1 = m, i1;
    for (i = 1; i < m; ++i) {
        i1 = 1;
        --m1;
        while (i1 <= m1) {
            if (a[i1][1] > a[i1 + 1][1])
                for (j = 0; j < n; ++j) {
                    a[i1][j] += a[i1 + 1][j];
                    a[i1 + 1][j] = a[i1][j] - a[i1 + 1][j];
                    a[i1][j] = a[i1][j] - a[i1 + 1][j];
                }
            ++i1;
        }
    }

    for (i = 0; i < m; ++i) {

        for (j = 0; j < n; ++j) {
            cout << a[i][j];

        }
        cout << endl;
    }
    return 0;

}
