#include <iostream>

using namespace std;
int main() {
    int n, m;
    float t = 0;
    cin >> n;
    cin >> m;
    int a[10][10];

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++)
            cin >> a[i][j];
    }


    for (int i = 0; i < m / 2; i++)
        for (int j = 0; j < n / 2; j++)
        {
             t = a[i, j];
            a[i, j] = a[m / 2 + i, n / 2 + j];
            a[m / 2 + i, n / 2 + j] = t;
        }

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++)
            cout << a[i][j];
        cout << "\n";
    }
    return 0;
}
